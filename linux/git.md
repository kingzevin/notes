# notes for git

## clone

* git clone --depth n `<repo>`:
  * 按照最近的n个commit进行clone
* git clone `<repo>` `<dir>`:
  * 把`<repo>`clone到`<dir>`目录下面
* git clone -b `<branch>` `<repo>`:
  * 只clone `<branch>` 分支
* 